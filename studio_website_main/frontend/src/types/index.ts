export interface ArchIcon {
  id: number;
  imageUrl: string;
  title: string;
  description: string;
}

export interface HeroState {
  activeIndex: number;
  title: string;
  description: string;
  bgImageUrl: string;
}
